import math.{
    abs,
    pow,
    sign,
    is_even,
    is_odd,
    is_positive,
    is_negative,
    is_zero,
    min,
    max,
    clamp
}

import int.{to_float, to_string}
import string.{len, is_empty}