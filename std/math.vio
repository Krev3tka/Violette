package math

func abs(n: float64) [float64] {
    if n >= 0.0 {
        return n
    }

    return -n
}

func sqrt(val: float64) [float64] {
    if val < 0.0 {
        return -1.0
    }

    if val == 0.0 || val == 1.0 {
        return val
    }

    var guess = val / 2.0
    let epsilon = 0.0000001

    while true {
        let next_guess = 0.5 * (guess + val / guess)
        if (next_guess - guess).abs() < epsilon {
            return next_guess
        }
        guess = next_guess
    }

    return guess
}

func pow(base: float64, exp: float64) [float64] {
    return base ** exp
}

func sign(n: int) [int] {
    if n < 0 {
        return -1
    } else if n == 0 {
        return 0
    }

    return 1
}

func is_even(n: int) [bool] {
    return n % 2 == 0
}

func is_odd(n: int) [bool] {
    return n % 2 == 1
}

func is_positive(n: int) [bool] {
    return n > 0
}

func is_negative(n: int) [bool] {
    return n < 0
}

func is_zero(n: int) [bool] {
    return n == 0
}

func min(this: int, other: int) [int] {
    if this < other {
        return this
    }

    return other
}

func max(this: int, other: int) [int] {
    if this >= other {
        return this
    }

    return other
}

func clamp(val: int, min: int, max: int) [int] {
    if val < min {
        return min
    }

    if val > max {
        return max
    }

    return val
}

func hypot(a: float64, b: float64) [float64] {
    return sqrt(a * a + b * b)
}