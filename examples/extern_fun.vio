extern func fabs(x: float64) [float64]
extern func sqrt(x: float64) [float64]
extern func floor(x: float64) [float64]

func main() {
    let negative = -64.0

    let positive = fabs(negative)
    println(positive)

    let root = sqrt(positive)
    println(root)

    let pi = 3.14
    println(floor(pi))
}