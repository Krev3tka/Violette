import math.sqrt

struct Point {
    x: float64,
    y: float64,
}

func distance(p: Point) [float64] {
    return sqrt((p.x * p.x) + (p.y * p.y))
}

func main() {
    let p = Point {
        x: 3,
        y: 4,
    }

    let dist = p.distance()

    println(dist)
}