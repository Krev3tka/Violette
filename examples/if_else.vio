package if_else

fun IfElse(x: float32) [float64] {
    if x > 5.0 {
        return x ** 2.0
    } else if x < 10.0 {
        return x ** 3.0
    } else {
        return x * 5.0
    }
}

fun main() {
    println(IfElse(6.0))
    println(IfElse(2.2))
    println(IfElse(19.0))
}